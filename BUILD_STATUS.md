# SaleTrack validation status

Checked in this workspace:

- four .NET 10 project files and matching solution references;
- valid JSON, XML, GitHub Actions YAML and GitLab CI YAML;
- six Blazor routes and balanced C# file structure;
- SQLite tables, unique serial-number rule and purchase-to-sale account flow;
- three easy account calculation tests are included;
- plain light-mode HTML with no JavaScript;
- Dockerfile and ZIP structure.

The workspace does not have the .NET SDK or Docker, so the compiled build, unit tests and Docker build must be run on the user's .NET 10 computer.
